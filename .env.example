# Generate with: caddy hash-password --plaintext 'your-password'
# Keep the single quotes: bcrypt hashes contain "$" characters.
CMS_BASIC_AUTH_HASH='$2a$14$replace-with-a-caddy-compatible-password-hash'
